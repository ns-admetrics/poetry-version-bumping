clean:
	poetry run pip uninstall poetry-version-bumping -y
	poetry run pip uninstall poetry-version-bumping -y
	poetry run pip uninstall poetry-version-bumping -y
	poetry run pip uninstall poetry-version-bumping -y
	poetry run pip uninstall poetry-version-bumping -y
	poetry run pip uninstall poetry-version-bumping -y
	poetry run pip uninstall poetry-version-bumping -y
